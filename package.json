{
	"name": "spacex-app",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"start": "vite",
		"build": "vite build",
		"preview": "vite preview",
		"deploy": "vite build --emptyOutDir && npm install gh-pages --save-dev && gh-pages -d dist"
	},
	"devDependencies": {
		"sass": "^1.97.3",
		"vite": "^7.3.1"
	},
	"dependencies": {
		"@popperjs/core": "^2.11.8",
		"bootstrap": "^5.3.8"
	}
}
